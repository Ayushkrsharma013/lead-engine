{
  "name": "proos-mcp-server",
  "version": "0.1.0",
  "description": "MCP server for LinkedIn ProOS — search leads, view stats, import from Apify, manage pipeline",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node --env-file .env dist/index.js",
    "dev": "tsx --env-file .env src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.13.1",
    "@supabase/supabase-js": "^2.105.4"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
