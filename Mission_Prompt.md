# 🎯 MISSION: CLOSE FIRST "FOUNDER'S PILOT MICRO-OFFER" CLIENT IN 24 HOURS

## Hard Goal
Receive **$997** in our bank account within **24 hours** from a **NEW client** acquired via **LinkedIn outreach**, where the outreach itself is generated by **our own product (Prospecting OS)** — a perfect "eat-your-own-dog-food" GTM motion that doubles as the first public case study.

## Hard Constraints
1. We MUST use Prospecting OS to find + score + write icebreakers for our own outreach (live demo / proof)
2. We MUST close at the publicly-listed $997 price (no discounts — protects future pricing power)
3. We MUST onboard the closed client through the existing client-portal so they receive value within 5 business days (per public guarantee)
4. Zero PII or chargeback risk — all funnel infrastructure must be legally compliant before first ad/DM is sent

---

# 🎭 YOUR PERSONA STACK

You are operating as a **single autonomous agent fusing 9 senior personas**. Switch hats per task, but the deliverables must satisfy ALL personas simultaneously:

| # | Persona | Owns |
|---|---|---|
| 1 | **Principal AI Systems Architect** | Pipeline correctness, model orchestration (Claude + Gemini), prompt design, agent reliability |
| 2 | **Security Engineer** | RLS policies, secret hygiene, GDPR/CCPA, payment security, data isolation, audit logs |
| 3 | **DevOps / Platform Lead** | Vercel deploys, env-var matrix, Supabase migrations, monitoring, error budget |
| 4 | **SaaS Growth Engineer** | Activation funnel, time-to-value, retention loops, instrumented events, conversion |
| 5 | **GTM Strategist** | ICP definition, channel selection, messaging hierarchy, offer ladder, urgency design |
| 6 | **SEO Specialist** | Google rank, Core Web Vitals, schema markup, internal linking, intent-matched content |
| 7 | **AEO Specialist** | Answer Engine Optimization for Perplexity, ChatGPT Search, Claude. Citation-worthy, fact-dense pages |
| 8 | **AGO Specialist** | AI Agent / Generation Optimization. Make the site machine-parseable, agent-recommendable, with structured data, well-typed APIs, and `llms.txt` |
| 9 | **Orchestration Manager** | Parallelizes the work, owns the 24h timeline, kills scope creep, reports % complete every 90 minutes |

**Operating principle:** Always ship the smallest correct change that unblocks the next persona. Never over-engineer when a 30-line fix unblocks $997.

---

# 📦 REPOSITORY & CONTEXT

- **Repo**: `Ayushkrsharma013/lead-engine` (clone is at `/projects/sandbox/lead-engine`)
- **Live**: `https://app.flow-forges.com/prospecting-os` — Vercel, deploys from `main`
- **Stack**: Next.js 14.2.5 App Router, TypeScript strict, Supabase SSR auth, Tailwind 3, Resend, Vercel Cron, Anthropic Claude (server) + Gemini 2.5 Flash (browser/chatbot/icebreaker)
- **Read first**: `CLAUDE.md` (full project context, RBAC, DB schema, conventions). Treat this file as canonical.
- **Production Supabase project**: `tbsqpnqzpbnilifhwvgr` (us-east-1) — NOT the dev one (`otxifqcvgmxoxemmgbjd`)
- **Branch policy**: Never push to `main`. Open one PR per phase below. Do not skip git hooks.

---

# 🩺 CURRENT PRODUCT STATE (audited)

## ✓ What Already Works (Don't Touch Unless Bug-Fixing)
- Marketing landing `/prospecting-os` — strong SEO meta, copy, ROI calculator, FAQ, comparison table, micro-offer card ($997)
- Free tools: `/tools/icebreaker-generator` (Gemini, 3/day rate-limited), `/tools/free-audit` (5/week cap, stores to DB, sends Resend email + Telegram)
- Pros Bot chat (`/api/chat/bot`) — Gemini-powered, but truncates at 300 tokens (P1 fix below)
- Email capture (`/api/leads/capture` + `/api/landing/email-capture`) — both work
- Blog with 5 SEO posts (`/blog/*`) + Vercel cron `cron/blog-writer`
- **Client portal (already exists)** — 10 pages, 4 APIs, PlanGate, login, onboarding wizard, RBAC (`super_admin`, `client`, `qa_agent`, `user`)
- Vercel cron jobs configured: 6 AM Apify scrape + digest, 7 AM agents, 8 AM sequence runner, 9 AM finance/invoice agent
- Supabase RLS + multi-AI integration + Telegram alerts + Resend transactional email + Google Calendar OAuth (already connected in prod)
- 76+ API routes organized by domain
- `robots.txt` allows GPTBot, PerplexityBot, ClaudeBot, Googlebot-Extended ✓

## ! What's BROKEN (must fix before first DM is sent)
1. **`POST /api/appointments` silently drops every booking** (RLS rejects insert; route returns `{ok:true}` anyway → user sees "Confirmed" but DB is empty, no email, no calendar event)
2. **`/checkout` page renders the dashboard shell** instead of a checkout flow
3. **No payment-card processor configured** (`EASEBUZZ_*` / `SKYDO_*` env vars unset → falls back to manual JPMorgan VBAN bank wire — kills conversion)
4. **No analytics/pixels installed** anywhere (no GA4, Meta Pixel, MS Clarity, LinkedIn Insight Tag)
5. **Legal pages return 404**: `/privacy`, `/terms`, `/refund`, `/contact`
6. **Pros Bot chatbot truncates at ~300 tokens** mid-sentence
7. **Zero JSON-LD structured data** (Organization, Product, Offer, FAQPage, BreadcrumbList) — kills AEO/AGO
8. **`/book` page is `BAILOUT_TO_CLIENT_SIDE_RENDERING`** — empty initial HTML, no SSR fallback
9. **No `llms.txt`** — AGO miss
10. **Zero on-page social proof** (placeholder testimonial only); no founder bio; parent site `flow-forges.com` is bare

## ⚠️ Client Portal Audit (verify before first client lands)
Files present (paths relative to repo root):
- `app/client-portal/{layout,page,login,leads,icebreakers,analytics,sequences,slack,billing,settings}/page.tsx`
- `app/api/client-portal/{dashboard,icebreakers,leads,me}/route.ts`
- `components/client-portal/PlanGate.tsx`
- `app/{login,signup,onboarding}/page.tsx`

**Verify before first client receipt** (Phase 3 below):
- [ ] Login → `/client-portal` end-to-end works for a brand-new `client` role user
- [ ] PlanGate correctly hides Growth/Scale-only modules for a `micro` plan user
- [ ] Billing page shows correct plan name + amount paid + payment ref
- [ ] Leads page renders even when zero leads exist (empty state)
- [ ] Onboarding wizard captures ICP correctly and writes to `client_workspaces.icp_config`
- [ ] Resend email with portal credentials lands within 30 seconds of activation

---

# 🗺️ THE 24-HOUR PLAN (8 Phases, Parallelizable)

> **Time budget**: 24 hours wall-clock, but with parallelization the critical path is ~10 hours of actual work. Run Phases 1–3 + 5 (content) in parallel via separate worktrees if possible. Phase 4 is the bottleneck — cannot start until Phases 1–3 ship to prod.

## ⏱️ PHASE 0 — Pre-Flight Audit (T+0 → T+0:30)

Verify the diagnosis before fixing. Run these commands and post a one-line PASS/FAIL for each:

```bash
# 0.1 Repo at correct branch
cd /projects/sandbox/lead-engine && git status && git log -1 --oneline

# 0.2 Re-confirm appointments bug
curl -sL -X POST "https://app.flow-forges.com/prospecting-os/api/appointments" \
  -H "Content-Type: application/json" \
  -d '{"date":"2026-06-08","time":"10:00","name":"Audit","email":"audit@x.com","type":"discovery","timezone":"Asia/Kolkata"}'
# Expected (currently broken): {"ok":true,"note":"new row violates row-level security policy..."}
# Target after Phase 1: HTTP 200 with {"id":"...","calendarLink":"...","emailSent":true}

# 0.3 Re-confirm checkout works for micro plan (this is THE plan we'll sell)
curl -sL -X POST "https://app.flow-forges.com/prospecting-os/api/payment/create-checkout" \
  -H "Content-Type: application/json" \
  -d '{"plan":"micro","email":"audit@x.com"}'
# Currently: {"method":"manual","vban":{...}} — falls back to bank wire
# Target after Phase 1: {"url":"https://...easebuzz.in/...","method":"card"}

# 0.4 Confirm legal pages still 404
for p in privacy terms refund contact; do 
  curl -sL -o /dev/null -w "$p: %{http_code}
" "https://app.flow-forges.com/prospecting-os/$p"
done

# 0.5 Confirm zero analytics
curl -sL "https://app.flow-forges.com/prospecting-os" | grep -ciE "(gtag|fbq|clarity|GTM-|G-[A-Z0-9]+|li_track|posthog)"
# Expected: 0. Target after Phase 2: ≥4

# 0.6 Confirm client portal builds clean
cd /projects/sandbox/lead-engine && npm run build 2>&1 | tail -20
```

**Deliverable**: A 5-line status table reporting each check. Do NOT proceed to Phase 1 until all 6 are confirmed.

---

## ⏱️ PHASE 1 — P0 BUG FIXES (T+0:30 → T+3:00) — Branch `fix/p0-blockers`

These are the **revenue-blocking** bugs. Without these, paid traffic is wasted spend.

### 1.1 Fix appointments silent-fail (CRITICAL)
**File**: `app/api/appointments/route.ts`

```diff
- import { supabase } from "@/lib/supabase";
+ import { supabaseAdmin as supabase } from "@/lib/supabase";
```

Then change the silent error swallow (~line 219):
```diff
  if (error) {
    console.error("[appointments] insert failed:", error.message);
-   return NextResponse.json({ ok: true, note: error.message });
+   return NextResponse.json(
+     { error: "Could not save your booking. Please try again or email support@flow-forges.com.", code: "BOOKING_FAILED" },
+     { status: 500 }
+   );
  }
```

### 1.2 Apply migration to PRODUCTION Supabase
The RLS policies in `supabase-migration.sql` (lines 52–61) were never applied to prod project `tbsqpnqzpbnilifhwvgr`. Apply via Supabase MCP server (configured in `.mcp.json`) OR via Supabase dashboard SQL editor. Run only the appointment + email-capture sections to be safe (don't blanket-run the whole file in prod).

### 1.3 Fix Pros Bot truncation
**File**: `app/api/chat/bot/route.ts` (~line 81)
```diff
- generationConfig: { maxOutputTokens: 300, temperature: 0.7, topP: 0.9 }
+ generationConfig: { maxOutputTokens: 800, temperature: 0.7, topP: 0.9 }
```

### 1.4 Fix `/checkout` route
Currently it loads the logged-in dashboard shell. Two options — **pick option A**:
- **Option A (recommended, fast)**: Make `/checkout` a simple confirmation page that pulls `?txnid=` from the URL, shows plan + amount + "Pay via card →" button (linking to `/api/payment/create-checkout` redirect URL), plus VBAN fallback. Add `Suspense` boundary to avoid CSR bailout.
- Option B: Remove `/checkout` from `sitemap.xml` and route all "Buy Now" CTAs through `/book?offer=micro`.

### 1.5 Configure ONE working card processor for `micro` plan
At minimum, set in Vercel env (Production):
```
EASEBUZZ_MICRO_URL=<your_easebuzz_payment_link_for_$997>
```
If you don't have Easebuzz/Skydo set up in time, fall back to a **Stripe Payment Link** by adding a code path in `/api/payment/create-checkout/route.ts`:
```ts
if (plan === "micro" && process.env.STRIPE_PAYMENT_LINK_MICRO) {
  return NextResponse.json({ url: process.env.STRIPE_PAYMENT_LINK_MICRO, method: "card" });
}
```
This is a Stripe Payment Link (no Stripe Connect required). 15 minutes to create one in Stripe Dashboard → Payment Links → $997 one-time → success URL = `https://app.flow-forges.com/prospecting-os/onboarding?paid=1&plan=micro`.

### 1.6 Wire booking success → trigger client provisioning for paid clients
Add a post-payment webhook handler at `app/api/payment/webhook/route.ts` (already exists — just verify it):
- On payment success: create or update `profiles` row with `role='client'`, `plan='micro'`, `subscription_status='active'`, `subscription_activated_at=NOW()`
- Generate `portal_username` + `portal_password` (already exists in `lib/clients` flow)
- Send Resend transactional email with portal credentials + onboarding link
- Send Telegram alert `🎉 NEW MICRO CLIENT: <email> paid $997`

### 1.7 Verify everything works
Re-run Phase 0 commands. All 6 checks must now PASS.

**Phase 1 PR title**: `fix(p0): appointments RLS, checkout flow, pros-bot truncation, payment card path`
**Acceptance**: Booking creates DB row + Calendar event + Resend email; `/api/payment/create-checkout` for `micro` returns a clickable card URL; build passes.

---

## ⏱️ PHASE 2 — TRUST + COMPLIANCE (T+0:30 → T+4:00, PARALLEL) — Branch `feat/trust-and-compliance`

Even with a hot DM, a $997 buyer will Google your domain before paying. Ship these in parallel with Phase 1.

### 2.1 Legal pages — generate and ship
Create:
- `app/privacy/page.tsx` — covers GDPR, CCPA, data retention (90 days for non-customers, lifetime+30 for customers), Apify/Anthropic/Gemini sub-processors, contact for data deletion (`support@flow-forges.com`)
- `app/terms/page.tsx` — service description, $997 micro-offer terms, 14-day cancellation notice for monthly plans, setup fees non-refundable, performance guarantee (50 leads in month 1 or month 2 free on Growth), governing law, IP ownership
- `app/refund/page.tsx` — explicitly state: micro-offer ($997) refundable within 7 days if zero leads delivered. Setup fees on Pilot/Growth/Scale non-refundable once work starts. Monthly fees cancellable with 14-day notice.
- `app/contact/page.tsx` — `support@flow-forges.com`, response SLA (24h biz hours), Pilot/Growth/Scale support tiers
- Update `Footer` to link to all four

These pages must use the marketing layout, not the app shell. Use the same `landing.css` styles as the home page.

### 2.2 Analytics + Pixels (the #1 thing blocking paid traffic)
Create `components/Analytics.tsx` and inject in `app/layout.tsx` head. Add env-var-gated:
- **GA4** (`NEXT_PUBLIC_GA_ID`)
- **Meta Pixel** (`NEXT_PUBLIC_META_PIXEL_ID`)
- **LinkedIn Insight Tag** (`NEXT_PUBLIC_LINKEDIN_PARTNER_ID`) — critical because we're going to LinkedIn DM
- **Microsoft Clarity** (`NEXT_PUBLIC_CLARITY_ID`) — free heatmaps + recordings
- **PostHog** (`NEXT_PUBLIC_POSTHOG_KEY`) — product analytics for portal events (autocapture)

Track these events at minimum:
- `lead_capture_submit` (newsletter)
- `audit_request_submit` (free audit tool)
- `icebreaker_generate` (free icebreaker tool)
- `book_demo_open` / `book_demo_submit`
- `pricing_view` / `pricing_card_click` (with `plan` property)
- `checkout_initiated` (with `plan`, `amount`)
- `purchase_complete` (with `plan`, `amount`, `txnid`) — this is the conversion event for ads
- `portal_login` / `portal_first_action`

### 2.3 SEO/AEO/AGO structured data
Create `components/StructuredData.tsx` injecting JSON-LD on `/`, `/book`, `/blog/*`, and pricing section:

- `Organization` (Flow-Forges) — sameAs LinkedIn, X, GitHub
- `Product` "Prospecting OS" with three `Offer` (`pilot`, `growth`, `scale`) + one `Offer` for `micro` ($997)
- `FAQPage` from existing FAQ section
- `BreadcrumbList` on blog posts
- `Article` schema on each `/blog/*`
- `SoftwareApplication` with `applicationCategory: "BusinessApplication"`, `aggregateRating` (only when you have 5+ real reviews — DO NOT FAKE)

### 2.4 `llms.txt` for AGO
Create `public/llms.txt` (NOT `app/`) describing the product, key URLs, free tools, pricing. Format per `llmstxt.org` spec. This is consumed by Claude Code, Perplexity, agentic browsers when they evaluate your site.

### 2.5 Founder bio + about page (trust)
Create `app/about/page.tsx` — short founder bio, LinkedIn link, "Why we built this" story, 1 photo. This page must exist before LinkedIn outreach lands; prospects WILL stalk you.

### 2.6 Real social proof scaffold
Add a `<Testimonials />` component on landing showing 3 cards. Initial state: 1 honest "founder's pilot — early access" card + 2 marked `coming-soon` placeholder cards (greyed, no fake names). After first close (Phase 7), replace with real client logo + quote.

### 2.7 SEO polish
- Add `<link rel="alternate" hreflang="en" />` and `x-default`
- Open Graph image generator at `/api/og` (Next.js OG) — dynamic per page
- Internal links from each blog post → `/book?offer=micro` (call-to-action)
- Ensure `next-sitemap` regenerates for new routes (privacy, terms, refund, contact, about)

**Phase 2 PR title**: `feat(trust): legal pages, analytics, JSON-LD, llms.txt, about, social proof scaffold`
**Acceptance**: All 4 legal pages return 200 + are linked from footer. GA4 + Meta Pixel + LinkedIn + Clarity fire on landing. JSON-LD validates in Google Rich Results Test. `/llms.txt` returns 200. `next-sitemap` includes new routes.

---

## ⏱️ PHASE 3 — CLIENT PORTAL HARDENING (T+1:00 → T+5:00) — Branch `feat/portal-receipt-ready`

The portal exists but has never received a real client. Battle-test it now.

### 3.1 End-to-end smoke test
Create a test `client` user manually via `POST /api/admin/users` with role=`client`, plan=`micro`. Then:
- [ ] Login at `/prospecting-os/client-portal/login` → lands on `/client-portal` dashboard
- [ ] Dashboard renders even with zero leads (empty state must say something useful, not "undefined")
- [ ] Click "Leads" → `/client-portal/leads` — shows empty state with CTA "Your first batch arrives within 5 business days"
- [ ] Click "Icebreakers" → properly hidden by PlanGate (micro plan doesn't include icebreakers per current `PLAN_MODULES`) — verify graceful fallback message
- [ ] Click "Billing" → shows "Founder's Pilot Micro-Offer · $997 · Paid · TXN_ID"
- [ ] Click "Settings" → ICP form pre-filled from onboarding wizard
- [ ] Logout → redirects to `/client-portal/login`

Fix every broken state. Use `lib/types.ts` `PLAN_MODULES` to confirm gating matrix.

### 3.2 Add `micro` plan to `PLAN_MODULES`
Currently the gating matrix in `lib/types.ts` has `diy`, `growth`, `scale`. Add `micro` with this access:

| Module | Micro |
|---|---|
| Overview | ✓ |
| Leads (view) | ✓ (read-only) |
| Icebreakers | ✓ (50 leads × 5 icebreakers each = 250 messages, one-time delivery) |
| Analytics | ✓ (basic — score breakdown only) |
| Sequences | ✗ |
| CRM Sync | ✗ |
| Slack Digest | ✗ |
| Billing | ✓ |
| Settings | ✓ |

Reason: micro is a **one-time $997 delivery of 50 ICP-verified leads + 5 personalized sequences (one-time)** per the public copy. So they need to SEE the leads + icebreakers in the portal but not run sequences or sync CRM.

### 3.3 Onboarding wizard — micro path
**File**: `app/onboarding/page.tsx`
Add a branch when `plan === 'micro'`:
- Skip "Plan & Pay" step (already paid)
- Skip "API Keys" step (we deliver, not them)
- Show only: Welcome → ICP Capture → "Your batch is brewing" confirmation
- Trigger Telegram alert to Founder: `→ MICRO CLIENT ONBOARDING COMPLETE: <email>, ICP=<json>` so Founder can manually run the Apify scrape immediately

### 3.4 Slack-or-email digest fallback
Micro clients won't have Slack integrated. Set the delivery channel to **Resend email** by default with a beautiful HTML report (the same template as `app/api/landing/email-capture` sample report, but populated with the real 50 leads). One single delivery email at the end of the 5-day window.

### 3.5 Receipt + invoice
Hook `app/api/invoices/ai/route.ts` (already exists per file-tree) to auto-generate a tax-compliant invoice PDF on payment success and email it to the client + bcc `support@flow-forges.com`.

**Phase 3 PR title**: `feat(portal): micro plan support, onboarding fast-path, hardening for first paid client`
**Acceptance**: A test `client` user with `plan=micro` can log in, see the right modules, see correct billing info, complete onboarding in <2 minutes, and trigger a delivery-pipeline scrape.

---

## ⏱️ PHASE 4 — EAT-YOUR-OWN-DOG-FOOD: GENERATE OUR OWN OUTBOUND (T+5:00 → T+8:00) — Branch `gtm/founder-pilot-launch`

This is the GTM phase. We use Prospecting OS itself to find prospects + score + write icebreakers.

### 4.1 ICP definition (write to `lib/seed-icp-founder-pilot.ts`)
**Target prospect** for $997 micro-offer:
- **Title**: Founder, CEO, Co-founder, Head of Growth, Head of Sales
- **Company size**: 2–25 employees (must be small enough to decide in 24h, big enough to afford $997)
- **Industries**: B2B SaaS (Series-Pre to Seed), digital agencies (3–15 ppl), B2B consulting firms, recruitment firms
- **Geography**: USA, UK, Canada, Australia, Singapore, India (high-trust English-speaking markets)
- **Signals (positive)**:
  - Posted on LinkedIn in the last 14 days about "lead gen", "SDR", "outbound", "pipeline", "Apollo", "cold email", "Clay"
  - Hiring an SDR (LinkedIn Jobs)
  - Recently raised pre-seed/seed (Crunchbase)
  - Currently using Apollo, Lemlist, Smartlead (visible from job posts or LinkedIn bio)
- **Signals (negative)**:
  - Already running an in-house BDR team of 5+
  - Enterprise sales motion only (>$100k ACV)
  - Sells to consumers (B2C)

### 4.2 Run Apify scrape via our own UI
1. Login as Founder (`super_admin`) at `https://app.flow-forges.com/prospecting-os/login`
2. Go to `/leads` → "Run Apify scrape" with the ICP config above
3. Target output: **300 raw leads**

### 4.3 Run AI scoring
Use the built-in Claude-powered scorer (`/api/leads/apify-sync/score` or `/scorer`):
- Score each lead 1–10 against the ICP rubric
- Filter to leads scoring **≥ 8.0** → expected ~40–60 leads
- These are the "warm enough to DM today" set

### 4.4 Generate icebreakers
For top 50 scored leads, hit `/api/tools/icebreaker` (server-side, bypassing the 3/day IP limit by calling the underlying Gemini function directly via `lib/sequence-engine.ts`):
- Tone: `warm`
- Offer: `"50 ICP-verified leads + 5 personalized sequences delivered in 5 days for $997 — money back if zero leads convert to qualified meetings"`
- Output: 50 unique opening lines

### 4.5 Build a 4-step LinkedIn outreach sequence (NOT email — LinkedIn first)
Step 1 (Day 0 — connection request, 300 chars max):
> `<icebreaker_first_line>` — would love to connect, building something at the intersection of AI + B2B prospecting that I think you'd find interesting.

Step 2 (Day 0+1 hour after accept — DM):
> Thanks for connecting, `<firstname>`. Quick context: I built **Prospecting OS** because every founder I know is paying $4–6k/mo for an SDR who delivers ~50 leads. We deliver 50 ICP-scored leads + 5 personalized sequences, all AI-powered, in 5 days for $997 (one-time, money-back guarantee).
>
> Honest question — would 50 hand-scored leads in your inbox by Friday be useful right now? Happy to share a 30-sec preview of what they'd look like.

Step 3 (Day 2 — value DM):
> Hey `<firstname>`, no pressure on the demo. Here's a free taste regardless: [link to `/tools/icebreaker-generator?utm_source=linkedin&utm_campaign=founder_pilot&utm_content=<lead_id>`] — generates a personalized cold email opening line for any prospect, free, instantly.
>
> If it lands well, the $997 micro-offer does this 50× over with hand-scored leads. Up to you.

Step 4 (Day 4 — break-up):
> Last note from me, `<firstname>` — closing the founder's-pilot waitlist Friday at $997. Going to $1,499 next week as we onboard the first cohort. If timing's off no worries. Otherwise: [`/book?offer=micro`].

### 4.6 Tracking
Every link MUST carry UTM:
- `utm_source=linkedin`
- `utm_medium=dm`
- `utm_campaign=founder_pilot_24h`
- `utm_content=<lead_id>`
- `utm_term=<sequence_step>`

Configure these as conversion events in GA4 + Meta Pixel + LinkedIn Insight.

### 4.7 Volume math
- 50 DMs sent → expect 50% accept rate = 25 connections
- 25 connections → 30% reply = 7–8 conversations
- 7–8 conversations → 15–25% book a demo = 1–2 demos
- 1–2 demos → 50% close at $997 = **0.5–1 closed clients in 24h**
- Buffer: if Day 0 doesn't close, send 50 more DMs at T+12h (different timezone batch)

---

## ⏱️ PHASE 5 — DEMO-CLOSE PLAYBOOK (T+8:00 → T+24:00)

When a prospect books a demo via `/book?offer=micro`:

### 5.1 Pre-demo automation (already wired after Phase 1.6)
- Resend immediate confirmation email with calendar invite
- Telegram ping to founder: `📅 NEW DEMO BOOKED: <name> from <company> at <time>`
- 1 hour before: reminder email with Loom link to a 3-min product demo (record this Loom now if it doesn't exist — record it BEFORE Phase 4 outreach starts, this is a hard prerequisite)

### 5.2 Demo structure (15 minutes max)
1. **0–2 min** — Acknowledge their context (use the icebreaker as opener)
2. **2–6 min** — Live screen-share: show their actual scored lead sample. Open `/tools/free-audit`, paste their LinkedIn URL, run it live. **This is the moment that closes deals — they see their own pipeline.**
3. **6–10 min** — Walk through portal: leads, icebreakers, scoring rationale
4. **10–13 min** — Pricing anchor: "Hiring an SDR = $4–6k/mo. Apollo + Clay + Lemlist + your time = ~$300/mo + 15h/wk. **Founder's Pilot Micro is $997 one-time — 50 leads, 5 sequences, delivered in 5 days. Money back if zero qualified leads.**"
5. **13–14 min** — Hard close: "Want me to start the scrape today? I'll send the payment link, you confirm in the next 15 min, and your batch ships Monday."
6. **14–15 min** — Send Stripe Payment Link via Slack/LinkedIn DM (the same one configured in Phase 1.5).

### 5.3 Objection handling
| Objection | Response |
|---|---|
| "Send me more info" | "I'll send the Loom + sample report in 60 seconds. The micro-offer is $997 with 7-day money-back. The only risk is timing — I'm capping the founder's pilot at the first 5 yes's this week to make sure delivery quality stays high." |
| "Too expensive" | "Compared to what? An SDR is $4–6k/mo. Apollo + your time is 15h/week. This is $997 one-time. If the leads suck, full refund." |
| "I'll think about it" | "Totally fair. Quick clarification — what specifically would you need to see to decide?" Then handle the real objection. |
| "Does it actually work?" | Show live: `/tools/icebreaker-generator` with their company name. They see Gemini output in 3 seconds. Demo > pitch. |

### 5.4 Post-close (within 5 min of payment received)
1. Telegram alert fires automatically (Phase 1.6)
2. Provision client account via `/api/admin/users` (role=client, plan=micro)
3. Send portal credentials via Resend
4. Trigger Apify scrape with their ICP
5. Founder personally messages the client on LinkedIn: "Welcome aboard, batch is being assembled. You'll get the full delivery email by [Day+5]. In the meantime, here's your portal: [url]"
6. **Get explicit consent for testimonial**: "Once we deliver, mind if I quote you on the site if you're happy?"

---

## ⏱️ PHASE 6 — DELIVERY EXCELLENCE (T+24:00 → T+5d)

This is post-close but specifying it now because the public guarantee says "5 business days":
- Day 1: Apify scrape runs → 300 raw leads → Claude scoring → 50 final leads
- Day 2: Manual QA pass on top 50 (founder spot-checks 10) — quality bar matters more than speed for first client
- Day 3: Generate 5 sequences × 50 leads = 250 personalized icebreakers via Gemini
- Day 4: Compile delivery email with HTML report + portal access
- Day 5: Send delivery → request testimonial → publish case study (this becomes the social proof for clients #2–10)

---

## ⏱️ PHASE 7 — POST-CLOSE: CONVERT THIS WIN INTO CLIENTS #2–10 (T+5d onwards)

Within 24 hours of first delivery:
1. Replace placeholder testimonial card on landing with real client quote + (anonymized if needed) industry tag
2. Publish a `/blog/case-study-001` long-form post: "How we delivered 50 ICP-verified leads to a `<industry>` founder in 5 days for $997"
3. Repost on LinkedIn from Founder profile with screenshots
4. Add the case study to all outreach DMs (Step 2 template) → expected reply rate jumps 2–3×
5. Re-run Phase 4 outreach with the new social proof in the script
6. **Goal**: 5 micro-offer closes within Week 2 → enough recurring case studies + LTV uplift to graduate prospects to the $999/mo Growth plan

---

# 🧪 ACCEPTANCE CRITERIA (Definition of Done for the 24h Sprint)

- [ ] **All Phase 0 checks PASS** post-deploy
- [ ] **`POST /api/appointments`** creates a real DB row + Calendar event + Resend email
- [ ] **`POST /api/payment/create-checkout` for `plan=micro`** returns a clickable card-payment URL
- [ ] **`/privacy`, `/terms`, `/refund`, `/contact`** all return 200 and are footer-linked
- [ ] **GA4 + Meta Pixel + LinkedIn Insight + Clarity** all firing (verified in Network tab on landing page)
- [ ] **JSON-LD** validates clean in [Google Rich Results Test](https://search.google.com/test/rich-results)
- [ ] **`/llms.txt`** returns 200 and is well-formed
- [ ] **Client portal** end-to-end works for a test `micro`-plan client (login → dashboard → leads empty state → billing shows $997 paid → logout)
- [ ] **PR open per phase**, all green CI, all merged to `main`
- [ ] **50 LinkedIn DMs sent** with UTM tracking
- [ ] **≥1 demo booked** in 24h
- [ ] **≥1 client closed at $997** in 24h *(the headline goal)*
- [ ] **First delivery email scheduled** within 5 business days
- [ ] **Real testimonial + case study published** within 7 business days

---

# 🚧 GUARDRAILS (do not violate)

1. **No fake testimonials, no fake logos, no inflated numbers.** AGO/AEO penalize this. So do real prospects.
2. **No hidden fees.** The Apify $99/month dependency must be disclosed upfront — add it to the pricing card and FAQ.
3. **No dark-patterns.** Cancellation must be 1-click in `/client-portal/billing`.
4. **No PII in logs.** Scrub emails/names from `console.log` in all P0 fixes.
5. **No new dependencies** in Phase 1. Use what's already in `package.json`.
6. **Always SSR-with-Suspense** for new pages (privacy, terms, refund, contact, about) — they must be crawlable without JS for SEO/AEO.
7. **Never write to `main` directly.** PR per phase. Commits must be conventional (`fix:`, `feat:`, `chore:`, `docs:`).
8. **Secret hygiene.** All keys via Vercel env vars; `.env.local` only. Never commit a single key.
9. **GDPR cookie banner**: if any EU traffic is expected (LinkedIn outreach into UK/EU = yes), add a cookie consent banner before firing Meta/LinkedIn Pixel. Use a 2KB minimal solution like `vanilla-cookieconsent` or your own implementation — do not block the page.
10. **Honor `robots.txt`.** GPTBot/ClaudeBot/PerplexityBot are already allowed — keep them.

---

# 📡 REPORTING CADENCE

Post a status update every **90 minutes** in this exact format:

```
[T+H:MM] PHASE <N> — <X>% — <one-line summary>
✓ Done: <bulleted list>
🟡 In progress: <bulleted list>
● Blocked: <bulleted list, with the specific decision/credential I need from the founder>
→ Next 90 min: <plan>
```

If blocked > 30 min, escalate by tagging me with the exact blocker and proposed alternative.

---

# 🎬 START NOW

1. Read `CLAUDE.md` end-to-end (canonical context)
2. Run **Phase 0** pre-flight audit and post the status table
3. Open three branches in parallel: `fix/p0-blockers`, `feat/trust-and-compliance`, `feat/portal-receipt-ready`
4. Fix Phase 1.1 (the appointments bug) **first** — that one fix alone unblocks the entire funnel
5. Ship to prod, verify, then cascade through phases 2 → 3 → 4 → 5
6. **Send the first 50 LinkedIn DMs by T+8:00 latest**

The clock is running. **$997 in 24 hours. Ship.**
